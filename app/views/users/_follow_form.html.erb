<% if current_user.following?(user) %>
  <%= form_with(model: current_user.active_follows.find_by(followed_id: user.id), local:true, method: :delete) do |form| %>
    <%= hidden_field_tag :user_id, user.id %>
    <%= form.submit t('views.common.unfollow') %>
  <% end %>
<% else %>
  <%= form_with(model: current_user.active_follows.build, local: true) do |form| %>
    <%= hidden_field_tag :user_id, user.id %>
    <%= form.submit t('views.common.follow') %>
  <% end %>
<% end %>
